const Dashboard = () => {
    return (
        <>
            <h2>You are signed in! </h2>

            <p>This is what the user will see when signed in.</p>
        </>
    );
};

export default Dashboard;
